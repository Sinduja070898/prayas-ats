# Frontend environment variables
# Copy to .env if needed:  cp env.example .env
# By default the dev server proxies /api to backend (see package.json "proxy").

# Optional: API base URL when not using proxy (e.g. production)
# REACT_APP_API_URL=http://localhost:5001
